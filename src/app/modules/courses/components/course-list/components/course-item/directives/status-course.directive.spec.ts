import { StatusCourseDirective } from './status-course.directive';

describe('StatusCourseDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusCourseDirective();
    expect(directive).toBeTruthy();
  });
});
